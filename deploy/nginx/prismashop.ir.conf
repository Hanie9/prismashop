# HTTP only — used until Let's Encrypt certs exist.
server {
    listen 80;
    server_name prismashop.ir www.prismashop.ir;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://prismashop.ir$request_uri;
    }
}
